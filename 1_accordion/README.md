# 🪗 Accordion Component (React + TypeScript)

A clean, reusable **Accordion UI component** built using **React**, **TypeScript**, and **Tailwind CSS**.  
It supports smooth open/close animations and is ideal for FAQs, help sections, or collapsible content.

---

## ✨ Features

- ✅ Built with **React + TypeScript**
- 🎯 Simple and readable component structure
- 🎨 Styled with **Tailwind CSS**
- 🔄 Smooth open / close animation
- 📱 Responsive and accessible-friendly
- 🔽 Uses `react-icons` for toggle indicators

---

## 🧠 How it works (Concept)

- The parent `Accordion` component holds the data.
- Each accordion item is rendered via a reusable `Accord` component.
- Local state (`isActive`) controls whether an item is expanded or collapsed.
- CSS transitions (`max-height`, `opacity`) handle the animation.

---

## 📦 Tech Stack

- **React**
- **TypeScript**
- **Tailwind CSS**
- **react-icons**



